// let a = "zunaira";
// let name = (a === "zunaira")? 'you print the right one':'you print the wrong one';
// console.log(name);

// let speed = 120;
// let echo = (speed <= 80) ? "slow":(speed > 80 && speed <=120) ? 'moderate':'fast';
// console.log(echo);

// var shift = "morning";
// let batch = (shift === "morning") ? "batch-10": "batch-11";
// console.log(batch);

// // let num = -10;
// // let check = (num === 10)? "a positive number":"a negative number";
// // console.log(check);

// // make nested checks

// let num2 = -10;
// let checking = (num2 > 0) ? "its a positive num":(num2 < 0) ? "a negative num":(num2 == 0) ? "";
// console.log(checking);
